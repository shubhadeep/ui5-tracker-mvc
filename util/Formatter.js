/*global window sap jQuery */
jQuery.sap.declare("sap.ui.demo.tracker.util.Formatter");

sap.ui.demo.tracker.util.Formatter = {
  isssueNameValueState: function (name) {
    "use strict";

    if (name)
  }
}
